<% layout('layout/boilerplate') %>



    <h1>Edit CampInn</h1>

    <form action="/campinns/<%=campinn._id%>?_method=PUT " method="POST">
        <div>
            <label for="title">Title</label>
            <input type="text" name="campinn[title]" id="title" value="<%= campinn.title%>">
        </div>
        <div>
            <label for="location">location</label>
            <input type="text" name="campinn[location]" id="location" value="<%= campinn.location%>">
        </div>
        <button>update campinn</button>
    </form>

    <a href="/campinns/<%= campinn._id%>">Back to campinn</a>
